@page
@model LotTrackerApp.Pages.PaisesPages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Paises[0].Descricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Paises) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CodPais">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CodPais">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CodPais">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
